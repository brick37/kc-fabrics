'use client'
import SeoHead from '../../components/SeoHead'
import { useState } from 'react'
export default function Contact(){ const [s,setS]=useState(''); const submit=async(e)=>{ e.preventDefault(); const form = Object.fromEntries(new FormData(e.target)); const res = await fetch('/api/contact',{method:'POST', body: JSON.stringify(form)}); if(res.ok) setS('Thanks — we received your message.'); }
 return (<><SeoHead title="Contact" /><section className="section"><div className="container md:grid grid-cols-2 gap-8"><div><h2 className="text-2xl font-semibold">Contact Us</h2><form onSubmit={submit} className="mt-6 space-y-4"><input name="name" className="w-full p-3 border rounded" placeholder="Name" required /><input name="email" className="w-full p-3 border rounded" placeholder="Email" type="email" required /><input name="phone" className="w-full p-3 border rounded" placeholder="Phone" /><textarea name="message" className="w-full p-3 border rounded" placeholder="Message" rows={6} required></textarea><button className="px-6 py-3 bg-primary text-white rounded">Send</button></form>{s && <div className="mt-4 text-green-600">{s}</div>}</div><div><h4 className="font-medium">Visit Us</h4><p className="text-sm text-gray-600 mt-2">Head Office: 123 Corporate Blvd, City</p></div></div></section></>) }
